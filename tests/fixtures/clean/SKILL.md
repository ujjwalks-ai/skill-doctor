---
name: clean
description: A deliberately well-formed skill used as a passing fixture for skill-doctor's own regression tests. Use this whenever the tests need a reference skill that clears every deterministic check — a valid name, a description long enough to carry a trigger surface, no embedded programs, and no hardcoded secrets — even if you do not say fixture.
---

# Clean

Do only what the delta requires. This body stays short, carries no inline
program, names no secret, and points at no bundled document, so every
skill-doctor check should come back clean.
